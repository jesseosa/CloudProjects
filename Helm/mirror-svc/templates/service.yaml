apiVersion: v1
kind: Service
metadata:
  name: {{ include "mirror-svc.fullname" . }}
  labels:
    app: {{ include "mirror-svc.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "mirror-svc.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 4004
      protocol: TCP
      name: http
